# 📌 Archivo `.env` (Variables de entorno)

Este archivo define variables de entorno que son utilizadas por `docker-compose.yml` y los servicios (MariaDB, WordPress, Nginx).  
Permite centralizar configuraciones y credenciales no sensibles para facilitar el despliegue.

---

## 🌍 Configuración general
- **`DOMAIN_NAME`** → Dominio asignado al proyecto (`frromero.42.fr`).  
- **`VOLUMES_ROOT`** → Ruta absoluta en el host donde se guardan los volúmenes persistentes (`/home/frromero/data`).  

---

## 📝 Configuración de WordPress
- **`WORDPRESS_SITE_TITLE`** → Título del sitio de WordPress (`frromero's inception website`).  
- **`WORDPRESS_ADMIN_USER`** → Nombre de usuario del administrador (`manager`).  
- **`WORDPRESS_ADMIN_EMAIL`** → Correo electrónico del administrador (`manager@42.fr`).  
- **`WORDPRESS_USER`** → Nombre del usuario estándar (`frromero`).  
- **`WORDPRESS_EMAIL`** → Correo electrónico del usuario estándar (`frromero@42.fr`).  

---

## 🗄️ Configuración de base de datos
- **`WORDPRESS_DB_USER`** → Usuario de la base de datos de WordPress (`wpuser`).  
- **`WORDPRESS_DB_NAME`** → Nombre de la base de datos (`wordpress`).  

⚠️ **Nota:**  
Las contraseñas (root, usuario DB y admin de WordPress) **no** se definen aquí por seguridad.  
En su lugar se gestionan mediante **Docker secrets** (`../secrets/*.txt`).  

---

✅ Con este archivo `.env`, se parametriza la configuración del proyecto para que sea fácilmente reutilizable en distintos entornos sin necesidad de modificar el `docker-compose.yml`.
